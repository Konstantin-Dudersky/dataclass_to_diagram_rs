use dataclass_to_diagram::domain::models::state_machine::{
    Diagram, State, Transition,
};

// fn test<'a>() -> Diagram<'a> {
//     let state1 = State::new("state1").build();

//     Diagram::new().add_state(&state1).build()
// }
